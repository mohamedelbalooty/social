import 'package:flutter/material.dart';

class UsersView extends StatelessWidget {
  static const String id = 'UsersView';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('UsersView'),
    );
  }
}
